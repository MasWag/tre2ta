[package]
name = "tre2ta-wasm"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
publish = false
description = "WASM bindings for tre2ta"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
tre2ta = { path = "../.." }
wasm-bindgen = "0.2.100"
